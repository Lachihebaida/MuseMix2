{# {% extends 'base.html.twig' %}
{% block title %}Nos Musiques{% endblock %}
{% block body %}
  <h1 class="text-left mx-auto my-5 pt-4">
        Nos Musiques
    </h1>
    <div class="d-flex justify-content-around"></div>
    {# On affiche toutes les musics avec une boucle #}
    {% extends 'base.html.twig' %}
{% block title %}Nos Musiques{% endblock %}
{% block body %}

  <h1 class="text-left mx-auto my-5 pt-4">
        Nos Musiques
    </h1>

    <div class="d-flex justify-content-around"></div>

    <div class="row mt-5">
    <div class="col-3 mb-3">
    <div class="card h-100 position-relative">
    <div class="position-relative">

    {# On affiche toutes les musics avec une boucle #}

    {% if music.imageName is defined and music.ImageName is not null %}
        <img src="{{ asset('/images/musics' ~ music.ImageName) }}" class="card-img-top" alt="{{ music.title }}">
        {% else %}
         <img src="{{ asset('/images/musics/image-default.png') }}" class="card-img-top" alt="image par défaut">
        {% endif %}

    {% if music.audioName is defined  and music.audioName is not null%}
        <audio controls src="{{asset ('/music/' ~ audio.audioName)}}" class="card-img-top" alt="..."></audio>
        {% else %}
        <audio controls src="{{ asset('/music/best-of-me15sec.mp3') }}" class="card-img-top" alt="image par défaut">
        {% endif %}

        

        {# <i data-feather="heart"></i> #}


        </div>
    
        <div class="d-flex gap-5 position-absolute bottom-0 m-4 p-3">
                
        </div>
    </div>
    </div>
</div>
{% endblock %}