{# {% extends 'base.html.twig' %}
{% block title %}Nos Musiques{% endblock %}
{% block body %}
  <h1 class="text-left mx-auto my-5 pt-4">
        Nos Musiques
    </h1>
    <div class="d-flex justify-content-around"></div>
    {# On affiche toutes les musics avec une boucle #}
    {% extends 'base.html.twig' %}
{% block title %}Nos Musiques{% endblock %}
{% block body %}

    <h1 class="mb-3"><i data-feather="search"></i> Nos Musiques<i data-feather="search"></i></h1>

    <div class="row">
    {% for music in musics %}

    <div class="d-flex justify-content-around"></div>

    <div class=" col-12 col-md-3 col-lg-3 mb-3">

<a href="{{ path ('app_music_show', {slug:music.slug }) }}" class="btn btn-dark btn-outline-secondary shadow">
                <img src="{{ asset('/images/musics' ~ music.ImageName) }}" class="card-img-top" alt="{{music.title}}">
                <h2>{{music.title}}</h2>
            </a>

</div>

{% endfor %}
    </div>
{% endblock %}







    