{% extends 'base.html.twig' %}

{% block title %}Edit Music{% endblock %}

{% block body %}

    {# {{ include('artist_music/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_profil') }}">back to list</a>

    {{ include('artist_music/_delete_form.html.twig') }} #}


	<a href="{{ path('app_profil') }}" class="btn btn-dark mb-3"><i class="align-middle" data-feather="arrow-left-circle"></i></a>
    <div class="d-flex flex-wrap justify-content-between align-items-center">
		<h1>Edit Music</h1>
        {{ include('artist_music/_delete_form.html.twig') }}
    </div>
    {{ include('artist_music/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}